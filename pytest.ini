[tool:pytest]
testpaths = tests/python
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --tb=short
    --strict-markers

markers =
    unit: pure unit tests (no I/O)
    integration: tests that spin up FastAPI TestClient
    slow: tests that take longer to run
